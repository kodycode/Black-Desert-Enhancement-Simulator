function displayAp(weaponId, desc, descAp, imgRight) {
  //will update this for accessory info?
  switch (obj[weaponId].enhanceRank)
  {
    case 1:
      if (obj[weaponId].itemClass === "dandelion")
      {
        descAp.text('Total AP: 22 ~ 31');
      }
      else if (obj[weaponId].itemClass === "kzarka")
      {
        descAp.text('Total AP: 22 ~ 26');
      }
      else if (obj[weaponId].itemClass === "liverto")
      {
        descAp.text('Total AP: 22 ~ 26');
      }
      break;

    case 2:
      if (obj[weaponId].itemClass === "dandelion")
      {
        descAp.text('Total AP: 25 ~ 34');
      }
      else if (obj[weaponId].itemClass === "kzarka")
      {
        descAp.text('Total AP: 25 ~ 29');
      }
      else if (obj[weaponId].itemClass === "liverto")
      {
        descAp.text('Total AP: 25 ~ 29');
      }
      break;

    case 3:
      if (obj[weaponId].itemClass === "dandelion")
      {
        descAp.text('Total AP: 28 ~ 37');
      }
      else if (obj[weaponId].itemClass === "kzarka")
      {
        descAp.text('Total AP: 28 ~ 32');
      }
      else if (obj[weaponId].itemClass === "liverto")
      {
        descAp.text('Total AP: 28 ~ 32');
      }
      break;

    case 4:
      if (obj[weaponId].itemClass === "dandelion")
      {
        descAp.text('Total AP: 30 ~ 39');
      }
      else if (obj[weaponId].itemClass === "kzarka")
      {
        descAp.text('Total AP: 30 ~ 34');
      }
      else if (obj[weaponId].itemClass === "liverto")
      {
        descAp.text('Total AP: 30 ~ 34');
      }
      break;

    case 5:
      if (obj[weaponId].itemClass === "dandelion")
      {
        descAp.text('Total AP: 32 ~ 41');
      }
      else if (obj[weaponId].itemClass === "kzarka")
      {
        descAp.text('Total AP: 32 ~ 36');
      }
      else if (obj[weaponId].itemClass === "liverto")
      {
        descAp.text('Total AP: 32 ~ 36');
      }
      break;

    case 6:
      if (obj[weaponId].itemClass === "dandelion")
      {
        descAp.text('Total AP: 35 ~ 44');
      }
      else if (obj[weaponId].itemClass === "kzarka")
      {
        descAp.text('Total AP: 35 ~ 39');
      }
      else if (obj[weaponId].itemClass === "liverto")
      {
        descAp.text('Total AP: 35 ~ 39');
      }
      break;

    case 7:
      if (obj[weaponId].itemClass === "dandelion")
      {
        descAp.text('Total AP: 38 ~ 47');
      }
      else if (obj[weaponId].itemClass === "kzarka")
      {
        descAp.text('Total AP: 38 ~ 42');
      }
      else if (obj[weaponId].itemClass === "liverto")
      {
        descAp.text('Total AP: 38 ~ 42');
      }
      break;

    case 8:
      if (obj[weaponId].itemClass === "dandelion")
      {
        descAp.text('Total AP: 43 ~ 52');
      }
      else if (obj[weaponId].itemClass === "kzarka")
      {
        descAp.text('Total AP: 43 ~ 47');
      }
      else if (obj[weaponId].itemClass === "liverto")
      {
        descAp.text('Total AP: 43 ~ 47');
      }
      break;

    case 9:
      if (obj[weaponId].itemClass === "dandelion")
      {
        descAp.text('Total AP: 48 ~ 57');
      }
      else if (obj[weaponId].itemClass === "kzarka")
      {
        descAp.text('Total AP: 48 ~ 52');
      }
      else if (obj[weaponId].itemClass === "liverto")
      {
        descAp.text('Total AP: 48 ~ 52');
      }
      break;

    case 10:
      if (obj[weaponId].itemClass === "dandelion")
      {
        descAp.text('Total AP: 53 ~ 62');
      }
      else if (obj[weaponId].itemClass === "kzarka")
      {
        descAp.text('Total AP: 53 ~ 57');
      }
      else if (obj[weaponId].itemClass === "liverto")
      {
        descAp.text('Total AP: 53 ~ 57');
      }
      break;

    case 11:
      if (obj[weaponId].itemClass === "dandelion")
      {
        descAp.text('Total AP: 58 ~ 67');
      }
      else if (obj[weaponId].itemClass === "kzarka")
      {
        descAp.text('Total AP: 58 ~ 62');
      }
      else if (obj[weaponId].itemClass === "liverto")
      {
        descAp.text('Total AP: 58 ~ 62');
      }
      break;

    case 12:
      if (obj[weaponId].itemClass === "dandelion")
      {
        descAp.text('Total AP: 63 ~ 72');
      }
      else if (obj[weaponId].itemClass === "kzarka")
      {
        descAp.text('Total AP: 63 ~ 67');
      }
      else if (obj[weaponId].itemClass === "liverto")
      {
        descAp.text('Total AP: 63 ~ 67');
      }
      break;

    case 13:
      if (obj[weaponId].itemClass === "dandelion")
      {
        descAp.text('Total AP: 68 ~ 77');
      }
      else if (obj[weaponId].itemClass === "kzarka")
      {
        descAp.text('Total AP: 68 ~ 72');
      }
      else if (obj[weaponId].itemClass === "liverto")
      {
        descAp.text('Total AP: 68 ~ 72');
      }
      break;

    case 14:
      if (obj[weaponId].itemClass === "dandelion")
      {
        descAp.text('Total AP: 73 ~ 82');
      }
      else if (obj[weaponId].itemClass === "kzarka")
      {
        descAp.text('Total AP: 73 ~ 77');
      }
      else if (obj[weaponId].itemClass === "liverto")
      {
        descAp.text('Total AP: 73 ~ 77');
      }
      break;

    case 15:
      if (obj[weaponId].itemClass === "dandelion")
      {
        descAp.text('Total AP: 78 ~ 87');
      }
      else if (obj[weaponId].itemClass === "kzarka")
      {
        descAp.text('Total AP: 78 ~ 82');
      }
      else if (obj[weaponId].itemClass === "liverto")
      {
        descAp.text('Total AP: 78 ~ 82');
      }
      break;

    case 16:
      if (obj[weaponId].itemClass === "dandelion")
      {
        descAp.text('Total AP: 86 ~ 95');
      }
      else if (obj[weaponId].itemClass === "kzarka")
      {
        descAp.text('Total AP: 86 ~ 90');
      }
      else if (obj[weaponId].itemClass === "liverto")
      {
        descAp.text('Total AP: 86 ~ 90');
      }
      break;

    case 17:
      if (obj[weaponId].itemClass === "dandelion")
      {
        descAp.text('Total AP: 94 ~ 103');
      }
      else if (obj[weaponId].itemClass === "kzarka")
      {
        descAp.text('Total AP: 94 ~ 98');
      }
      else if (obj[weaponId].itemClass === "liverto")
      {
        descAp.text('Total AP: 94 ~ 98');
      }
      break;

    case 18:
      if (obj[weaponId].itemClass === "dandelion")
      {
        descAp.text('Total AP: 106 ~ 115');
      }
      else if (obj[weaponId].itemClass === "kzarka")
      {
        descAp.text('Total AP: 106 ~ 110');
      }
      else if (obj[weaponId].itemClass === "liverto")
      {
        descAp.text('Total AP: 106 ~ 110');
      }
      break;

    case 19:
      if (obj[weaponId].itemClass === "dandelion")
      {
        descAp.text('Total AP: 114 ~ 123');
      }
      else if (obj[weaponId].itemClass === "kzarka")
      {
        descAp.text('Total AP: 114 ~ 118');
      }
      else if (obj[weaponId].itemClass === "liverto")
      {
        descAp.text('Total AP: 114 ~ 118');
      }
      break;

    case 20:
      if (obj[weaponId].itemClass === "dandelion")
      {
        descAp.text('Total AP: 122 ~ 131');
      }
      else if (obj[weaponId].itemClass === "kzarka")
      {
        descAp.text('Total AP: 122 ~ 126');
      }
      else if (obj[weaponId].itemClass === "liverto")
      {
        descAp.text('Total AP: 122 ~ 126');
      }
      break;

    default:
      break;
  }

  document.getElementById(desc).style.display = 'block';
  document.getElementById(desc).style.left = (imgRight) + "px";
}
